# if using postgresql
DB_DRIVER=postgres
DB_SOURCE=postgres://username:password@localhost:5432/dbname?sslmode=disable
SERVER_PORT=8080
JWT_SECRET=your-secret-key

# Database Configuration
DB_DRIVER=mysql
DB_SOURCE=username:password@tcp(localhost:3306)/dbname?charset=utf8mb4&parseTime=True&loc=Local

# Server Configuration
SERVER_PORT=8080

# JWT Configuration
JWT_SECRET=your-very-secret-key-32-characters-long

# (Optional) Timezone Configuration
TZ=UTC